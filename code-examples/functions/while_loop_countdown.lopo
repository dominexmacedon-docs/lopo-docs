define collectCountdown = func(startValue) {
  define tracker = [];
  while (startValue > 0) {
    push(tracker, startValue);
    startValue--;
  }
  return tracker;
};
show collectCountdown(3);